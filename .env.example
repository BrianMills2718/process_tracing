# Required: Gemini API key for the default model (gemini-2.5-flash)
# Set either GEMINI_API_KEY or GOOGLE_API_KEY (both work with LiteLLM)
GEMINI_API_KEY=your-gemini-api-key-here
GOOGLE_API_KEY=your-google-api-key-here

# Optional: Override the default model (default: gemini-2.5-flash)
# Accepts any LiteLLM-compatible model identifier
# PT_MODEL=gemini-2.5-pro

# Optional: Legacy model override (PT_MODEL takes precedence)
# GEMINI_MODEL=gemini-2.5-flash
