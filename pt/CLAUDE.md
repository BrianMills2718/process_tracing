# pt

Active `process_tracing` implementation.

This subtree contains the production pipeline, schema contracts, LLM boundary,
and reporting logic. Keep the system LLM-first for semantic interpretation,
keep pure math deterministic, and route prompt assets through `pt/prompts/`.
