# pt/multi

Module entrypoints for multi-document cross-case analysis.

Use this subtree for the `python -m pt.multi` surface and multi-case CLI
packaging. Core cross-case orchestration still lives in neighboring `pt/`
modules such as `multi_pipeline.py`; keep this directory focused on the module
entry contract.
