# config

Configuration surfaces for `process_tracing`.

Keep model choices, thresholds, and operational policy here rather than
hardcoding them in `pt/`. Config changes are behavioral changes because they
alter the inference pipeline and evaluation conditions.
