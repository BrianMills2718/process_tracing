# docs/validation

Validation utilities and benchmark scripts for `process_tracing`.

Use this subtree for focused validation passes that test whether extraction,
diagnostic scoring, or methodological claims hold on real cases. If a script
starts becoming part of the shipped pipeline, move it into `pt/` or another
runtime subtree.
