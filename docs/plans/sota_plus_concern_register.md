# Plan 003 - SOTA+ Concern Register

This is the live concern register for
`docs/plans/003_sota_plus_execution_master_plan.md`.

Use it for uncertainties, tensions, audit findings, recommendations, and
methodology risks that surface during implementation. Do not leave durable
concerns only in chat. Add entries as they arise, then triage the register at
every slice boundary before moving on.

Do not delete entries. Append new concerns as new rows; update status and
disposition in place when an item is triaged.

## Disposition Values

- `open` - not yet addressed.
- `resolved` - fixed and verified.
- `mitigated` - reduced enough to proceed, with remaining risk stated.
- `accepted` - intentionally left as known risk, with rationale.
- `escalated` - blocks progress until re-planned or decided.
- `deferred` - assigned to a named future slice.

## Register

| ID | Status | Slice | Concern | Why it matters | Disposition / next action |
|---|---|---|---|---|---|
| C-001 | mitigated | 0 | The independent critique path depends on the assistant harness that Slice 0 is supposed to build. | Until the harness exists, critique must use a fallback such as Claude/Codex through an existing tool path, a human review, or a deliberately separate adversarial run. | Slice 0 attempted Claude review, but Claude CLI was not authenticated. Fallback adversarial review is documented in `evidence/current/Evidence_Plan003_Slice0_AssistantHarness.md`; future slices should use the assistant harness or authenticated Claude/Codex review when available. |
| C-002 | deferred | 1 | The Brumaire source packet is a strong first case, but one case cannot calibrate PhD-quality thresholds. | A single historical case can validate shape and artifact quality, but benchmark thresholds need multiple cases and planted failures. | Defer threshold calibration to Slice 10; use Slice 1 only to validate source-packet contract and corpus-cap behavior. |
| C-003 | mitigated | 0 | Live E2E assistant tests may require provider access, budget, and local Codex/Claude Code runtime availability. | A gate that cannot run in normal development will become performative. | Deterministic tests validate delegation, metadata, artifact persistence, and dependency boundaries. Live provider smoke is gated behind `PT_RUN_LIVE_AGENT_TESTS=1` and was not required for default `make check`. |
| C-004 | open | 2 | Hypothesis partition quality is partly exploratory: exact MECE thresholds may be hard to specify before observing benchmark failures. | Over-tight rules can reject useful historical explanations; loose rules allow broad absorptive winners. | Slice 2 should define structural blockers deductively and use benchmark readouts for threshold calibration. |
| C-005 | resolved | 1 | The assistant emits a draft source-packet artifact, but the pipeline does not yet consume a source-packet contract. | Without Slice 1, source-packet drafting can still remain adjacent to inference rather than governing it. | Implemented `pt/source_packet.py`, `--source-packet`, `ProcessTracingResult.source_packet`, report source-packet visibility, and audit distinctions for no packet vs packet gaps vs stale synthesis. Verified by source-packet, CLI, pipeline, report, and audit tests. |
| C-006 | mitigated | 4 | Source-packet acceptance does not by itself prove that packet sources were acquired or that extracted evidence covers every source group. | A packet can govern research design while the input text still omits sources or underuses key groups; treating packet metadata as evidence would recreate the overclaim problem. | Packet-source coverage now reports exact-marker input/evidence coverage in `result.json`, report, and audit. Live run `output/live_plan003_dual_audit_20260623_001` caught Source C present in input but absent from evidence; source-aware extraction fixed that in `output/live_plan003_source_aware_extract_20260623_002` with 5/5 accepted sources covered and Source C at 4 evidence items. Remaining risk is claim-scope source acquisition and high-priority missing-source resolution; future work must acquire or explicitly accept missing source classes such as private correspondence. |
| C-007 | resolved | 1 | Deterministic mocked gates were treated as sufficient for Slice 1 completion. | Mocked tests missed a live refinement failure where Pass 5 produced evidence-to-hypothesis support edges inside `new_causal_edges`, causing `apply_refinement` to fail. This is exactly the class of issue live E2E testing catches. | Added a mandatory live non-mocked E2E gate to `CLAUDE.md` and Plan 003. Tightened Pass 5 schema/prompt to forbid hypothesis IDs as causal-edge endpoints, added `tests/test_pass_refine.py`, reran live Brumaire E2E with `gpt-5-mini`, and audited the live result. |
| C-008 | resolved | 1 | Live refined Pass 3 can return structurally valid likelihood output with overlapping dependence clusters. | Overlapping clusters double-count evidence or crash the live E2E after expensive refinement, and JSON schema cannot enforce cross-cluster set disjointness. | Added deterministic Pass 3 validation-repair: one explicit retry with the exact validation error, then fail loud if still invalid. Verified by `TestVectorCompleteness.test_repairs_overlapping_clusters_once_with_validation_feedback`; live run `_002` triggered the repair once and then completed with 43/43 evidence vectors and 6 valid dependence clusters. |
| C-009 | mitigated | 1 | Bourrienne memoir evidence partially mitigates but does not resolve the high-priority private-correspondence gap. | Retrospective insider memoir evidence improves planning-sequence observability, but agency and bargaining claims can still change if contemporaneous correspondence among Sieyes, Bonaparte, Lucien, Murat, and allies is found. | Added `SourceGapDisposition` and Source F so the report/audit says `partially_mitigated` instead of silently clearing the gap. Live E2E `output/live_plan003_source_expansion_20260623_001` verified Source F extraction with 8 evidence items, 6/6 source coverage, and claim-scope cap still active. Direct correspondence acquisition remains future work unless the limitation is explicitly accepted. |
| C-010 | mitigated | 1 | Search automation can create a false sense that the source gap is solved. | An acquisition plan and live search hits are only retrieval guidance; they are not evidence until a source is fetched, assessed, added to the packet/input corpus, extracted, and rerun through the trace. | Added trace-derived acquisition targets in `pt.source_acquisition` and `make source-acquisition`. The target planner ranks source gaps, absences, sensitivity, and driver-corroboration needs, but the high-priority correspondence gap remains unresolved until a source is acquired or explicitly dispositioned. |
| C-011 | resolved | 1 | Active architecture docs lacked the design-plan-required boundary, domain-model, and data-flow diagrams. | Without diagrams, boundaries and contracts can drift behind prose, especially now that the system includes static reports, a workbench server, source acquisition, and external retrieval. | Added `docs/ARCHITECTURE.md` with Mermaid boundary, domain-model, and data-flow diagrams, typed contract/failure tables, and the backward runtime pass. Added `tests/test_architecture_docs.py` to keep the diagrams present. |
| C-012 | mitigated | 1 | The latest design-plan skill requires synthetic mockups for significant UI/seam boundaries and notebooks for non-trivial contract work, but some existing workbench/source-acquisition surfaces predate that explicit gate. | Retrofitting a pre-implementation mockup is impossible after implementation, but ignoring the gate would let future UI/seam changes repeat the same ambiguity. | Added `docs/DESIGN_PLAN_COMPLIANCE_AUDIT.md` and a Plan 003 universal mockup/notebook gate. Future significant UI or cross-seam contract slices must include the mockup/notebook artifact or record an explicit waiver in the slice plan. |
| C-013 | mitigated | 5 | Archiving the current workbench immediately could discard a working, tested source-acquisition surface before the replacement host preserves its capabilities. | The workbench predates the latest mockup/notebook gate, but it is still active tested functionality. Archiving it now would remove useful behavior and evidence rather than resolve the design-process gap. | Plan 005 keeps `pt.workbench` active as a narrow source-acquisition surface until the interactive trace execution host replaces or folds in equivalent behavior. Archive only after replacement passes live E2E and the archive rationale is written. |
| C-014 | mitigated | 3 | The active docs could be read as if the source packet were the end-state methodology rather than the first contract in a larger source-design, trace-production, and mixed-method bridge. | That would undershoot the SOTA+ thesis and reward document completeness over methodological convergence. | Updated the SOTA review, theory/goals doc, recovery plan, and execution master plan so the active frontier is explicit: source packet -> source-design engine, first-class trace-production modeling, stronger dependence handling, and a benchmarked within-case -> cross-case bridge. |
