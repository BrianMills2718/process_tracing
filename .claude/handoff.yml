updated: "2026-06-25T04:02:48"
project: "process_tracing"
summary: "Shipped the interactive trace execution host, verified it live through refine, and updated the planning/progress docs."

completed:
  - "Added `pt/trace_host.py` to manage run creation, stage-by-stage replay, artifact persistence, and typed run state (commit b936c29)"
  - "Extended `pt/workbench.py` with JSON APIs and a browser UI for create-run, stage replay, run inspection, and acquisition controls (commit b936c29)"
  - "Added deterministic and live host coverage in `tests/test_workbench.py` and hardened Pass 3 validation repairs in `pt/pass_test.py` (commit b936c29)"
  - "Updated `docs/plans/005_interactive_trace_execution_host.md` and `docs/progress/plan003_sota_plus_progress.md` to reflect the live host slice (commit b936c29)"
  - "Ran live non-mocked Brumaire host E2E on `output/workbench_runs/run_20260624_200616_d4f6` through refine, producing `result.json`, `report.html`, and `refinement.json` (commit b936c29)"

pending:
  - id: P1
    priority: high
    effort: large
    summary: "Build the visual-audit slice for the host"
    details: "Implement the next Plan 005 slice: a temporal causal DAG or layered timeline, dependence-cluster overlays, an evidence inventory/provenance view, and a robustness/sensitivity readout. Start from the completed host APIs and use the live Brumaire run as the reference artifact."
  - id: P2
    priority: medium
    effort: medium
    summary: "Decide the archive policy for the old acquisition workbench"
    details: "The current host still exposes acquisition behavior. Keep the old surface available until the new host has equivalent acquisition coverage and a live E2E proof, then decide whether `pt.workbench` stays as an alias or is archived with a rationale."

warnings:
  - "The live model path is slow and quota-sensitive; short smoke checks are not enough to prove convergence. Wait for run status and on-disk artifacts."
  - "`docs/plans/CLAUDE.md` is generated by the plan-status sync script; do not hand-edit it."
  - "`AGENTS.md` is a generated mirror in this repo; update canonical `CLAUDE.md` / plan docs instead."

sanity_checks:
  - cmd: "make check"
    expect: "exits 0"
  - cmd: "python -m pt.workbench --help"
    expect: "prints usage text and exits 0"
  - cmd: "test -f output/workbench_runs/run_20260624_200616_d4f6/report.html"
    expect: "exits 0"
