# models

Theory or causal-model inputs consumed by the multi-case pipeline.

Keep model files here declarative and reviewable. These are analysis inputs,
not generated outputs, so changes should stay synchronized with the commands
and examples documented in repo root `CLAUDE.md`.
